export type FormValues = {
    email: string;
    password: string;
};

export type FormFieldTypes = {
    title: string;
    htmlFor: string;
    type: string;
    inputName: string;
    placeholder: string;
};

export type MinLengthTypes = {
    value: number;
    message: string;
}

export type FormSubmitTypes = {
    title: string;
};