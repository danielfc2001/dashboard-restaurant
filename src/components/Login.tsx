import { FC } from "react";

const Login: FC = () => {
  return (
    <div className="flex flex-row justify-center items-center w-full h-screen max-h-screen text-white/5 dark:text-white">
      Login
    </div>
  );
};

export default Login;
